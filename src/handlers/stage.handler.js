import { getDefaultData } from "../init/defaultData.js";
import { getStage } from "../models/stage.model.js";

//이건 스테이지 선택
export const getSelectStage = (uuId) => {};
//선택 창에 원하는 스테이지 값 받으면서 클리어 현황 반환
export const getUserStage = (uuId) => {};
